<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Todo_list"
        tools:targetApi="31">
        <activity
            android:name="com.siyuan.todolist.activities.MainActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.siyuan.todolist.activities.TaskDetailActivity"
            android:label="任务详情" />
        <activity
            android:name="com.siyuan.todolist.activities.SettingsActivity"
            android:label="设置" />
        <activity
            android:name=".activities.TaskEditActivity"
            android:exported="false" />
    </application>

</manifest>
